# Copy to .env in this deploy/ directory.
# Both domains' DNS A records must already point at this EC2 instance's public IP before you
# start Caddy, or the Let's Encrypt HTTP-01 challenge will fail and it'll retry/backoff.

FRONTEND_DOMAIN=ranking.kwtc.example
API_DOMAIN=api.ranking.kwtc.example
